<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>
  <style>
    /* Reset & Body */
    * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Poppins', sans-serif; }
    body {
      background: linear-gradient(135deg, #6189aa, #c73d89);
      background-size: 400% 400%;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
      color: #333;
      animation: gradientShift 15s ease infinite;
    }

    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Navbar */
    .navbar {
      width: 100%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 30px;
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(15px) saturate(180%);
      -webkit-backdrop-filter: blur(15px) saturate(180%);
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
      border-radius: 0 0 15px 15px;
      position: fixed;
      top: 0;
      left: 0;
      z-index: 100;
      color: #fff;
      font-weight: 600;
    }
    .navbar .logo { font-size: 1.2rem; }
    .navbar .greeting { font-size: 1rem; }

    /* Add padding to avoid overlap with fixed navbar */
    body { padding-top: 70px; }

    /* Heading */
    h1 {
      margin: 20px 0;
      color: #fff;
      font-size: 2rem;
      text-shadow: 2px 2px 10px rgba(0,0,0,0.4);
      letter-spacing: 1px;
    }

    /* Form Styling - Glassmorphism */
    form {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(15px) saturate(180%);
      -webkit-backdrop-filter: blur(15px) saturate(180%);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
      display: flex;
      flex-direction: column;
      width: 100%;
      max-width: 400px;
      margin-bottom: 30px;
      padding: 30px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    form:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 40px rgba(0,0,0,0.35);
    }

    /* Labels */
    label {
      margin-bottom: 8px;
      font-weight: 600;
      color: #040735;
      text-shadow: 0px 1px 2px rgba(0,0,0,0.1);
    }

    /* Inputs */
    input[type="text"] {
      padding: 14px;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.3);
      background: rgba(255,255,255,0.25);
      color: #000;
      margin-bottom: 20px;
      transition: all 0.3s ease;
    }
    input[type="text"]::placeholder { color: rgba(0,0,0,0.6); }
    input[type="text"]:focus {
      border-color: #000dff;
      box-shadow: 0 0 12px rgba(0,13,255,0.4);
      outline: none;
    }

    /* Buttons - Gradient & Shadow */
    button {
      padding: 14px;
      background: linear-gradient(135deg, #000dff, #6b73ff);
      color: #fff;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      font-size: 16px;
      font-weight: 600;
      transition: all 0.3s ease;
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }
    button:hover {
      background: linear-gradient(135deg, #6b73ff, #000dff);
      transform: translateY(-3px) scale(1.02);
      box-shadow: 0 10px 25px rgba(0,0,0,0.3);
    }

    /* URL Generated Message */
    p {
      margin-bottom: 20px;
      color: #830303;
      font-weight: bold;
      text-align: center;
      text-shadow: 1px 1px 3px rgba(0,0,0,0.3);
    }

    /* Table Styling - Modern */
    table {
      width: 100%;
      max-width: 900px;
      border-collapse: collapse;
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0,0,0,0.25);
      background: rgba(255,255,255,0.2);
      backdrop-filter: blur(10px);
      margin-bottom: 30px;
      transition: transform 0.3s ease;
    }
    th, td {
      padding: 14px 18px;
      text-align: center;
      transition: all 0.3s ease;
    }
    thead { background: linear-gradient(90deg, #000dff, #6b73ff); color: #fff; }
    tbody tr:nth-child(even) { background: rgba(255,255,255,0.1); }
    tbody tr:hover {
      background: rgba(255,255,255,0.25);
      transform: scale(1.02);
      box-shadow: 0 4px 15px rgba(0,0,0,0.15);
    }

    /* Links */
    a {
      color: #880516;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.3s ease;
    }
    a:hover { color: #060b64; text-decoration: underline; }

    /* Responsive */
    @media (max-width: 600px) {
      form, table { width: 100%; }
      h1 { font-size: 1.6rem; }
      th, td { font-size: 13px; padding: 10px; }
    }
  </style>
</head>
<body>
  <!-- Navbar with personalized greeting -->

  <h1>URL Shortener</h1>

  <form method="post" action="/url">
    <label for="url">Enter Your Original URL:</label>
    <input type="text" name="url" id="url" placeholder="https://example.com" required/>
    <button type="submit">Generate</button>
  </form>

  <% if (locals.id) { %>
    <p>
      URL Generated: <a href="/url/<%= id %>">http://localhost:8001/url/<%= id %></a>
    </p>
  <% } %>

  <% if (locals.urls && urls.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>Sr. No.</th>
          <th>Short URL</th>
          <th>Redirect URL</th>
          <th>Clicks</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% urls.forEach((url, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td><a href="/url/<%= url.shortID %>"><%= url.shortID %></a></td>
            <td><%= url.redirectURL %></td>
            <td><%= url.visitHistory.length %></td>
            <td><%= url.createdAt.toLocaleString() %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>
